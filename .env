# Copia este archivo como ".env" y completa tus datos reales.
# NUNCA subas el archivo ".env" real a un repositorio público.

# Servidor SMTP (ejemplo con Gmail)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Cuenta que ENVÍA el correo (necesita una "contraseña de aplicación", no tu contraseña normal)
SMTP_EMAIL=carlos.nakidg@gmail.com
SMTP_PASSWORD=igzq wyhv wbhf rokt

# Cuenta que RECIBE la notificación cuando acepten el plan
DESTINATION_EMAIL=carlos.nakidg@gmail.com
